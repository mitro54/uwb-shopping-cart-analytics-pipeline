bytebuddies_dbt:
  outputs:
    dev:
      type: duckdb
      path: '../data/warehouse/dev.duckdb'
      external_root: '../data/gold'
      extensions:
        - httpfs
        - parquet
      threads: 1
  target: dev
